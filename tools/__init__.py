"""Maintainer tooling (not part of the runtime image). Run from the repo root, e.g.:

    python -m tools.build_catalog            # build the whole curated catalog
    python -m tools.build_catalog --collection impressionism --limit 5
"""
